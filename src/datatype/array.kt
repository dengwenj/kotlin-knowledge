package datatype

fun main() {
    var arr = intArrayOf(1, 2, 4, 5, 6, 7, 8, 9, 10);
    // 创建数组的方式
    var arrStr: Array<String> = arrayOf("朴睦", "李雷")
    for (s in arrStr) {
        println(s)
    }
}